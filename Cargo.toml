[package]
name = "tinyrts"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
rand = "0.9"

# Optimize deps in dev for playable framerates
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
